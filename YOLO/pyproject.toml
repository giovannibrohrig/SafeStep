[project]
name = "yolo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Giovanni Bautto Rohrig", email = "giovanni.rohrig@aluno.cps.sp.gov.br" }
]
requires-python = ">=3.14"
dependencies = [
    "ultralytics>=8.4.143",
]

[project.scripts]
yolo = "yolo:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
